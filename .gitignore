# Credenciais - NUNCA versionar
*.env
.env
service-account*.json
gcp-key*.json
*.pem
*.key
profiles.yml

# Ambiente Python
.venv/
venv/
__pycache__/
*.pyc

# dbt
target/
logs/
dbt_packages/

# Dados brutos baixados do portal (não versionar dado, só código).
# Os seeds do dbt (dbt/seeds/*.csv) NÃO entram aqui — são dado de
# referência fixo (código, não dado baixado) e precisam estar
# versionados, senão dbt build quebra pra quem clonar o repo.
/dados/

# SO / editor
.DS_Store
Thumbs.db
.vscode/
